# 设置管理员目录访问规则
<Files "admin_*">
    Order allow,deny
    Allow from all
</Files>

# 禁止直接访问 PHP 文件（除了入口文件）
<FilesMatch "^(?!index\.php|api\.php|admin_login\.php).*\.php$">
    Order deny,allow
    Deny from all
</FilesMatch>
